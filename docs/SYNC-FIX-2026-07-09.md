# Sync fix — 2026-07-09

Журнал разбора расхождения: рабочее устройство (A) показывало 91 пациента,
второе устройство (B, v1.3.1) после синхронизации — 81.

## Бэкапы

- `~/backups/dental-patients/data-cloud-2026-07-09-91patients.json` — снимок облака
  на момент разбора: 91 пациент, `updatedAt: 2026-07-08T14:56:19.185Z`.
  Это единственный полный слепок базы. Восстановление — PUT этого файла в
  `Яндекс.Диск:/dental-patients/data.json`.

## Диагноз

Облако было в порядке: 91 пациент, все реальные, дублей `id` нет, демо-записей нет.
`updatedAt` облака совпадал с `createdAt` последнего пациента — значит A пушил исправно.

Недостающие на B 10 записей — ровно 10 последних по `createdAt` (7–8 июля).
B держал снапшот по состоянию на ~6 июля и **считал себя синхронизированным**.

Механика залипания: `pull()` сравнивает `updatedAt` целого снапшота. Если B получает
по HTTP устаревший ответ, его локальный `updatedAt` равен `remoteAt` этого ответа →
ветка `else` → `setState('idle')`. Данные не меняются, push не запускается
(`localAt` не больше `remoteAt`), UI рапортует успех. Состояние устойчивое.

## Найденные дефекты

1. **GET без анти-кэш заголовков** (`syncManager.ts`, `webdav()`).
   На Android `fetch` идёт через OkHttp с HTTP-кэшем. Яндекс.Диск на GET отдаёт
   `ETag`/`Last-Modified` без `Cache-Control`, клиент вправе применить эвристическую
   свежесть и вернуть старый снапшот. Основная гипотеза по симптому B.

2. **`loadData()` выдавал снапшоту без метки текущее время** (`storage/index.ts`).
   `updatedAt: parsed.updatedAt ?? new Date().toISOString()` — такой снапшот на каждом
   запуске получал `now()`, всегда выигрывал LWW и затирал облако.

3. **`schedulePush()` делал слепой PUT** мимо demo-защиты (`syncManager.ts`).
   `pull()` умеет `hasRealData()`, а `schedulePush` — нет. Сценарий потери: B стартует
   офлайн (pull → `offline`), врач трогает любую запись → `persist()` заливает
   демо-снапшот поверх 91 боевого пациента.

4. **`runExclusive()` тихо выбрасывал операцию** (`if (running) return;`), без ретрая.
   Push, совпавший по времени с pull, терялся до следующего запуска приложения.

5. **LWW по всему снапшоту**, без слияния по сущностям. Два устройства, правившие разных
   пациентов, теряют правки одного из них. Исправлено в 1.4.0 — см. ниже.

## Изменения (пункты 1–4)

`src/sync/syncManager.ts`
- `webdav()`: на GET добавлены `Cache-Control: no-cache, no-store`, `Pragma: no-cache`,
  `cache: 'no-store'` и cache-buster `?ts=<epoch>` в URL (кэш OkHttp ключуется по URL,
  одних заголовков мало). PUT/MKCOL не тронуты. Пятый параметр `cacheBust` позволяет
  запросить чистый URL.
- `pull()`: 404 больше не означает сразу «снапшота нет». Сначала повторный GET **без**
  cache-buster. Если сервер отдаёт файл по чистому URL — работаем с ним. Иначе, если и
  там 404 — push локального. Страховка на случай, если Яндекс.Диск сочтёт `?ts=` частью
  имени файла: без неё устройство B залило бы свои 81 поверх 91 через 404-ветку.
- `schedulePush()`: вместо `doPush(data, auth)` вызывает `pull(data)` — тот отдаёт push
  только когда локальное реально новее, либо облако демо/пусто.
- `runExclusive()`: слот `pending` вместо тихого дропа; конкурирующая операция
  вытесняет предыдущую отложенную (актуален только последний снапшот) и выполняется
  после текущей.

`src/storage/index.ts`
- `loadData()`: `?? new Date(0).toISOString()` вместо `new Date().toISOString()`.
  От затирания пустым удалённым снапшотом защищает `hasRealData()` в `pull()`.

## Trade-off, который приняли

`schedulePush` → `pull` означает, что push проходит только при `localAt > remoteAt`.
Если системные часы устройства отстают от времени последней записи в облаке, его
изменения не зальются, а локальные данные будут перезаписаны облаком. Раньше слепой
PUT заливал в любом случае. Обмен корректности на устойчивость к затиранию — сознательный.
Если у врача сбиты часы, симптом будет «мои правки не уезжают».

## Результаты проверки WebDAV (2026-07-09)

Прогон curl против боевого Диска подтвердил диагноз и уточнил два решения.

**Кэш-гипотеза подтверждена.** Ответ Яндекса на GET:
```
HTTP/1.1 200 OK
ETag: a14797de3ea9fa235f375d0fdbaaeb0b
Last-Modified: Thu, 09 Jul 2026 09:59:14 GMT
```
`Cache-Control` отсутствует. OkHttp в этом случае вправе применить эвристическую
свежесть и отдать телефону старый ответ, не сходив в сеть. Это и есть механизм залипания B.

**Cache-buster `?ts=` работает.** Первый прогон дал `HTTP 000` — это был сетевой сбой,
а не отказ сервера. Повтор: `?ts=`, `?cb=`, `?ts=` по HTTP/1.1 — все `HTTP 200`.
Вывод «Яндекс не переваривает query» был неверен и снят.

**ETag-ревалидация тоже работает** (план Б): `If-None-Match: <актуальный etag>` → `304`,
`If-None-Match: <чужой>` → `200`.

**Почему оставили `?ts=`, а не ревалидацию.** Кэш OkHttp адресуется по строке URL:
меняющийся URL — гарантированный поход в сеть, ни один слой не подсунет старое.
Ревалидация полагается на то, что HTTP-клиент честно исполнит `Cache-Control: no-cache`.
Обычно исполняет, но именно на молчаливом кэше мы уже обожглись, и ставить сохранность
базы в зависимость от чужой добросовестности не хочется. Цена — 400 КБ полной загрузки
вместо лёгкого `304`; при добавлении пациента всё равно уходит PUT того же объёма.
404-страховка оставлена: шесть строк против риска затирания базы, если Яндекс однажды
передумает насчёт query.

## Признак того, что B активно затирал облако

`Last-Modified` файла — 9 июля 09:59, а `updatedAt` внутри файла — 8 июля 14:56.
Кто-то 9 июля записал в облако содержимое с внутренней отметкой от 8 июля. При штатной
работе так не бывает: заливающий телефон ставит свежую отметку.

Сходящееся объяснение: B заливал наверх свою отставшую базу (81 запись), а A при
следующем запуске видел, что его копия новее, и возвращал 91 обратно. Пинг-понг, в
котором A пока побеждал. Заливать B мог из-за дефекта 2 (`loadData` выдавал снапшоту
без метки `now()`, и тот всегда выигрывал LWW) в связке с дефектом 3 (слепой PUT).

Отсюда: **ставить на B версию 1.3.1 нельзя** — в ней оба дефекта живы. Только 1.3.2.

## Как проверить

`npx tsc --noEmit` → 0 ошибок (сделано).
Тест-раннера в проекте нет, автотестами эти правки не покрыты.

Живая проверка на устройстве B (только v1.3.2, НЕ 1.3.1):
1. Снести приложение целиком («Удалить», не «Очистить данные» — пароль синка живёт
   в expo-secure-store и очистку данных переживает).
2. Убедиться, что сеть работает. Если B стартует офлайн и врач тронет запись,
   `persist()` дёрнет push — в 1.3.1 это залило бы демо-снапшот поверх боевой базы.
3. Поставить 1.3.2, сразу зайти в настройки синка, не трогая список пациентов.
   Чистая установка идёт по ветке `remoteReal && !localReal`, принимает облако,
   ничего не пушит. Должно показать 91.
4. Затем обновить A до 1.3.2 поверх, без удаления: данные в AsyncStorage переживут
   обновление, `updatedAt` у A свежий.

Проверка кэш-гипотезы без устройства (нужен логин + пароль приложения Яндекса):
```bash
curl -s -u "LOGIN:APP_PASSWORD" https://webdav.yandex.ru/dental-patients/data.json \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(len(d['patients']), d['updatedAt'])"
```

## Откат

```bash
git revert <sha>          # точечно
git checkout main         # ветка fix/sync-stale-snapshot не влита
```
Если облако пострадало — PUT бэкапа обратно:
```bash
curl -u "LOGIN:APP_PASSWORD" -T ~/backups/dental-patients/data-cloud-2026-07-09-91patients.json \
  https://webdav.yandex.ru/dental-patients/data.json
```

## 1.4.0: слияние по сущностям (пункт 5) и своя подпись

### Схема v2

`AppData.tombstones: Tombstone[]` — общий список `{id, deletedAt}`. ID — uuid, глобально
уникальны, поэтому отдельные списки по типам не нужны. Миграция v1 → v2: `tombstones: []`.

`sync/merge.ts`:
- union коллекций по `id`, при конфликте побеждает бо́льшая метка **сущности**
  (не снапшота); пациенты сливаются рекурсивно по notes/appointments/questionnaires/files;
- файлы неизменяемы, метка — `addedAt`;
- надгробие убивает сущность, если `deletedAt >= updatedAt`. Правка **после** удаления
  воскрешает запись: для медданных потерять правку хуже, чем воскресить;
- `pruneNested()` фильтрует вложенные и у пациента, пришедшего лишь с одной стороны
  (для него `mergePatient` не вызывается);
- GC надгробий: `TOMBSTONE_TTL_DAYS = 90`, к сроку оба устройства заведомо синхронизировались;
- `canonical()` — сериализация с сортировкой ключей, чтобы понять, изменилось ли что-то,
  и не гнать лишний PUT на 400 КБ.

`pull()` больше не арбитражит по `updatedAt`: считает merge, применяет локально, если
отличается от локального, и пушит, если отличается от удалённого. Снапшот с
`schemaVersion` выше нашей не трогает вовсе (`error`) — иначе потеряли бы незнакомые поля.

`resetAll` **намеренно без надгробий**: это локальный сброс, а не удаление базы у всех.
Пустой снапшот не проходит `hasRealData()` и получает облако обратно.

**Первый запуск после обновления даст один лишний PUT**: merge сортирует коллекции по `id`,
порядок в облаке изменится однократно. Дальше `canonical` совпадает, PUT не идёт.

### Проверка

`npm run check:merge` — 20 проверок (`scripts/merge-check.js`), включая прогон на боевом
снапшоте из `~/backups/dental-patients/`: отставший телефон 81 + облако 91 = 91; пациент,
добавленный офлайн на B, доезжает; ни один пациент не теряет заметок и анкет.
Путь к снапшоту переопределяется через `REAL_SNAPSHOT=...`; без него эти тесты пропускаются.

### Подпись

До 1.3.2 release подписывался **отладочным** ключом (`signingConfigs.debug`), лежащим в репо.
С 1.4.0 — собственный ключ RSA 4096, срок 10000 дней:

```
keystore: ~/keystores/dental-patients-release.jks
креды:    ~/keystores/dental-patients-release.credentials   (chmod 600, НЕ в git)
alias:    dental-patients
отпечаток SHA-256: a97787ccf5ec1c87373f448de2afc2ec7ac64e3e3d1d337f66883a9540c82bf7
(старый debug-ключ был fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c)
```

`android/app/build.gradle` читает креды из этого файла; путь переопределяется через
`-PdentalCredentials=...`. Если файла нет — сборка release падает с внятной ошибкой.

**Смена ключа необратима для установленных копий.** Android не ставит APK с другой
подписью поверх: 1.4.0 требует удалить приложение на обоих телефонах.

**Потеря keystore = невозможность обновлять приложение.** Сделайте копию
`~/keystores/` на внешний носитель. Каталог `android/` в `.gitignore`, поэтому правка
`build.gradle` живёт только локально: после `expo prebuild --clean` её надо вернуть.

## Нижний отступ списков (1.4.1 → 1.4.4)

Последняя карточка списка уходила под таб-бар. Ушло четыре сборки, потому что
проверить правку без устройства нельзя, а гипотезы были правдоподобные и неверные.

Что оказалось **не** причиной:
- дублирующая кнопка «Добавить» на экране пациентов (убрана в 1.4.0 — правка сама
  по себе верная, но перекрытие давала не она);
- конфликт shorthand `padding` с `paddingBottom` в одном объекте (1.4.1);
- отсутствие `style={{flex:1}}` у списков (1.4.2 — тоже верная правка, но не эта).

Что оказалось причиной: **`ListFooterComponent` во `FlatList` не давал зазора**, тогда
как `SectionList` в расписании с тем же кодом зазор рисовал. Причина расхождения не
выяснена. Решение — механизм, работающий на устройстве: `paddingBottom` в
`contentContainerStyle` (1.4.3), затем подбор величины: 160dp не хватало, бар резал
карточку на ~41dp, итог `TAB_BAR_FAB_LIST_PADDING` = 144, отступ 224dp (1.4.4).

Метрики в `src/navigation/tabBarMetrics.ts`. Все списки используют
`getFabListBottomPadding`, `getListBottomPadding` осталась для ScrollView-экранов.

**Проверка бандла grep'ом бесполезна.** `index.android.bundle` — Hermes bytecode,
из него не извлекаются даже обычные строки (`strings` не находит «Пациенты»).
Проверяемы: исходники, `npx tsc --noEmit`, факт сборки, `assets/app.config` внутри APK
(там лежит `version`), подпись через `apksigner verify --print-certs`.

Версия на экране настроек берётся из `Constants.expoConfig?.version`, руками не правится.

## Пока не починено — держать в голове

- `schedulePush` c debounce 2.5 с: при уходе в фон/убийстве процесса таймер не
  отрабатывает, push теряется до следующего запуска. Нужен флаш на `AppState` → `background`.
- Слияние не разрешает конфликт двух правок одной сущности: побеждает та, у которой
  метка свежее, вторая теряется молча. Для полей одного пациента это приемлемо,
  для одновременного редактирования одной заметки — нет.
- В базе дубль по ФИО: «Нефедова Ольга Ивановна», два разных `id`
  (`2026-07-07T13:18` и `2026-07-07T14:56`). Похоже на случайный повторный ввод, не баг синка.
